return {
    "stevearc/dressing.nvim", -- Improves UI for vim.input and vim.select with nice floating popups
    -- Makes pop-up menus and input boxes look cleaner and easier to use

    event = "VeryLazy", -- Load only when needed (e.g. when a plugin or LSP uses a prompt)
}
